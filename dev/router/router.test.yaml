supergraph:
  listen: 0.0.0.0:4000

cors:
  origins:
    - http://localhost:3001
  allow_credentials: true
  allow_headers:
    - content-type

override_subgraph_url:
  afl: http://host.docker.internal:8180/query
  ffl: http://host.docker.internal:8181/query
